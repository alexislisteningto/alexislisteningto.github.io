{
  "name": "alex-is-listening-to",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && wrangler dev --live-reload",
    "typecheck": "tsc --noEmit && wrangler types --env-file .dev.vars.example --check",
    "test": "vitest run",
    "check": "npm run typecheck && npm test && npm run build && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@types/node": "^26.1.2",
    "typescript": "latest",
    "vitest": "^4.1.10",
    "wrangler": "^4.116.0"
  }
}
